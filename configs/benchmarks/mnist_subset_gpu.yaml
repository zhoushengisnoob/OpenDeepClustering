estimator: DEC
seeds: [0]
dataset:
  kind: mnist
  root: ../../datasets
  download: false
  split: all
  subset_size: 10000
  subset_seed: 0
  normalization: dec
parameters:
  n_clusters: 10
  dims: [500, 500, 2000, 10]
  pretrain_method: greedy
  layerwise_pretrain_epochs: 2
  pretrain_epochs: 5
  pretrain_optimizer: sgd
  pretrain_lr: 0.1
  optimizer: sgd
  lr: 0.01
  max_epochs: 5
  update_interval: 40
  batch_size: 256
  deterministic: true
  device: cuda
output: ../../benchmark_outputs/mnist_subset_gpu.json
